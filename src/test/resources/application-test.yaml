spring:
  datasource:
    url: jdbc:h2:mem:demo
    username: sa
    password: sa
  devtools.restart.enabled: false

logging:
  level:
    com:
      sun:
        faces: trace
    javax:
      faces: trace
